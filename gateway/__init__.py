# Gateway / data plane (placeholder — Phase 1+)
#
# OpenAI-compatible HTTP edge: authn via tenant API key, route to
# Ollama (local) or vLLM (prod), enforce tenant isolation. No admin UI.

__version__ = "0.0.0-phase0"
