# packages/

Shared libraries for the monorepo.

| Package | Role |
|---------|------|
| `contracts` | OpenAI-compatible Pydantic models; consumed by gateway |
| `tenantkit` | Tenant filesystem isolation + API key registry helpers |

Install smoke/lint deps from repo root:

```bash
pip install -r requirements-dev.txt
```
